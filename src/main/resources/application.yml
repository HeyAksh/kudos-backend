spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Apple_db
    username: postgres
    password: heyaksh
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type: TRACE
